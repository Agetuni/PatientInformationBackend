namespace PatientInformation.Api.Registrars
{
    public class ApplicationLayerRegistrar : IWebApplicationBuilderRegistrar
    {
        public void RegisterServices(WebApplicationBuilder builder)
        {
            //builder.Services.AddAutoMapper(typeof(Program), typeof(CountryCreate));
            //builder.Services.AddMediatR(typeof(CountryCreate));

        }
    }
}
